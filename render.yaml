services:
  - type: web
    name: charcha-backend
    runtime: node
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: FRONTEND_URL
        value: https://chrcha-ai.web.app
